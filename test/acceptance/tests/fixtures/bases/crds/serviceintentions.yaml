apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: intentions
spec:
  destination:
    name: svc1
  sources:
    - name: svc2
      action: allow
    - name: svc3
      permissions:
        - action: allow
          http:
            methods:
              - GET
              - PUT
            pathExact: "/foo"
